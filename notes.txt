
    source myenv/bin/activate
    export FLASK_DEBUG=1
    export FLASK_ENV=development
    server run dlpl-server flask run 
    Avoid using spaces in names (project name = project 1, for example).
